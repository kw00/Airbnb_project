extends ../layout

block content
  h1 글 작성
  
  form(action='/memo', method='POST', enctype="multipart/form-data")
    .form-group
      label(for='email') 이메일
      input.form-control(name='email' placeholder='이메일을 입력하세요.')
    .form-group
      label(for='title') 제목
      input.form-control(name='title' placeholder='숙소명을 입력하세요..')
    .form-group
      label(for='content') 내용
      textarea.form-control(name='content' placeholder='후기 및 별점(5개)을 입력하세요.')
    .form-group
      label(for='images') 이미지 1
      input.form-control(type="file", name='photos')
    .form-group
      label(for='images') 이미지 2
      input.form-control(type="file", name='photos')
    .form-group
      label(for='images') 이미지 3
      input.form-control(type="file", name='photos')
    .form-action
      a.btn.btn-default(href='javascript:window.history.back();') 뒤로 
      | &nbsp;
      button.btn.btn-primary(type='submit') 글작성 
  
